## 题目描述
假定你是李华。

你今年决定选择一些学校去上学。

有$n$个学校，每个学校有一个编号（依次为$1$到$n$）和一个名称（用数字来表示），编号为$i$的学校名称为$a_i(0\le a_i<2^m)$（名字互不相同）。

你至少要去一个学校上学。

如果你要去好多个学校上学，你就需要使用分身术，但若你选择的学校中按编号排序后相邻的四个学校名字异或和为$s$你的分身术就会失效。

求有多少种方案不需要分身术或分身术不会失效。

形式化地，若你选择的学校编号依次为$p_1,p_2,p_3,...,p_k(1\le p_1< p_2< ...< p_k\le n)$，若存在$i(1\le i\le k-3)$使得$a_{p_i}\oplus a_{p_{i+1}} \oplus a_{p_{i+2}} \oplus a_{p_{i+3}}=s$（$\oplus$表示异或），则该序列不合法。

求在所有$2^n-1$个可能的序列中问有多少个序列合法。

你只需要输出答案$\mod 998244353$之后的结果即可。
## 输入格式：
第一行两个正整数$n,m,s$，其中$m$表示$a_i$的范围。

接下来一行$n$个互不相同的非负整数$a_i$如题所述。
## 输出格式：
一行一个整数表示答案$\mod 998244353$之后的结果。
### 样例输入1：
```
5 3 3
2 3 4 7 6 
```
### 样例输出1：
```
30
```
### 样例$2\sim 3$：
见[下发文件]()
## 数据范围：
$1\le n\le 4000,1\le m\le 12,0\le a_i,s<2^m$

